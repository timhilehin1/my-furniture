import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
	title: "wishlist",
	description: "Wishlist",
};

export default function Products() {
    return <div>Productsss</div>;
}